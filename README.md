# AClIC Chatbot - Frontend

This folder contains the frontend part of the AClIC Chatbot application, built using modern JavaScript and React (or other stack you're using).

## 🚀 Getting Started

To run the frontend locally:

```bash
cd client
npm install
npm run dev